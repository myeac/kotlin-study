package oop.abstraction

/*

if 2 classes share common featuers, create a super class that contains those features as inherit
use intefaces

abstract keyword ---- means class cannot be instantiated, only extended FROM

reduce code duplication
structures code vbased on real words concepts
relate some func, methods and classes

 */